const express = require("express")
const bodyParser = require("body-parser")
const app = express()
const router = express.router()
const UseDatabase = require("./Database.js")

function InsertTitleFormToDatabase()
{
    
}